/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    // Add your Supabase Storage host here once images are uploaded, e.g.
    // remotePatterns: [{ protocol: 'https', hostname: 'xxxx.supabase.co' }],
    remotePatterns: [],
  },
};

export default nextConfig;
